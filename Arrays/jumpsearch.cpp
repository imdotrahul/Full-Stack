#include<iostream>
#include<algorithm>
using namespace std;


void jumpsearch(int arr[],int numberofelements, int blocksize)
{
    int n = 
    while(i >0)
    {
        
    }

}

int main()
{
    int numberofelements;
    cin>> numberofelements;

    int arr[numberofelements];

    for(int i = 0;i<numberofelements;i++)
    {
        cin>>arr[i];
    }
    sort(arr,arr+n);
    int blocksize = 3;
    jumpsearch(arr,numberofelements,blocksize);
}